// Extras (e.g. "Extra calamansi") are stored as regular products under this
// category — the backend has no dedicated Extra model, so this is how the
// frontend tells them apart from regular menu items wherever products are
// listed (vendor dashboard, buyer-facing storefront).
export const EXTRA_CATEGORY = "Extras";
